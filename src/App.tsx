import{useState}from"react";import{usePaintEstimate}from"./features/paint-estimator/hooks/usePaintEstimate.ts";import{WallInputs,WallCountSelect,CoatsSelect,CanSizeSelect,CutoutInputs,ResultSummary}from"./features/paint-estimator/components.tsx";export default function App(){const{wallCount,setWallCount,walls,updateWall,coats,setCoats,doorCount,setDoorCount,windowCount,setWindowCount,canType,setCanType,result,hasAnyWall,wallError,clearAll}=usePaintEstimate() as any;const[aa,bb]=useState(false);return<div className="p-4"><h1>Paint Estimator</h1><button onClick={()=>bb(!aa)}>Clear</button>{aa&&<div><button onClick={()=>{clearAll();bb(false)}}>Yes</button><button onClick={()=>bb(false)}>No</button></div>}<WallCountSelect wallCount={wallCount} onChange={setWallCount}/><WallInputs wallCount={wallCount} walls={walls} onUpdate={updateWall} wallError={wallError}/><CoatsSelect coats={coats} onChange={setCoats}/><CutoutInputs doorCount={doorCount} windowCount={windowCount} onDoor={setDoorCount} onWindow={setWindowCount}/><CanSizeSelect canType={canType} onChange={setCanType}/><ResultSummary result={result} hasAnyWall={hasAnyWall}/></div>}
